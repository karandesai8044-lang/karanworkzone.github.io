version: "3.9"

services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: print-kiosk-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: 8000
      PYTHONUNBUFFERED: "1"
    restart: unless-stopped
